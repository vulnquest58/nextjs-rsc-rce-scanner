# 🔍 VQ-R2S Scanner  
### ماسح أمني لاكتشاف واستغلال ثغرات **RSC RCE** في تطبيقات **Next.js**  

![Next.js Security](https://img.shields.io/badge/Next.js-%23RSC%20RCE-red?logo=next.js&logoColor=white)
![Python](https://img.shields.io/badge/Python-3.6%2B-blue?logo=python)
![License](https://img.shields.io/badge/License-MIT-green)

أداة مسح متقدمة لاكتشاف و**التحقق التفاعلي** من ثغرات التنفيذ البعيد (RCE) الناتجة عن سوء تكوين ميزة **React Server Components **(RSC) في تطبيقات **Next.js** (خاصة الإصدارات < 14.2.4).  
مصممة للاستخدام الأخلاقي فقط في سياقات الاختبار المصرح بها.

## 🎯 الميزات الرئيسية

- ✅ **اكتشاف تلقائي** لتطبيقات Next.js عبر تحليل الاستجابات والـ Headers.
- ✅ **التحقق التفاعلي من الثغرة** باستخدام حمولات استغلال واقعية مبنية على تقارير أمنية حقيقية (مثل CVE-2024-51378).
- ✅ **وضع تفاعلي آمن** لتنفيذ أوامر محدودة مسبقًا: `id`، `ls`، `env`، `whoami`، وغيرها.
- ✅ **لا يُنفّذ أي حمولة خطيرة** — جميع الأوامر معرفة مسبقًا وغير مدمرة.
- ✅ دعم كامل **للأوان** في PowerShell، CMD، وLinux/macOS.
- ✅ تقارير تلقائية بصيغ **JSON** و**HTML** للتوثيق والمشاركة.
- ✅ لا يتطلب أي تبعيات خارجية معقدة — يعمل بكامل وظائفه من ملف واحد.

## ⚠️ التحذير الأخلاقي

> **هذا الأداة مخصصة للاستخدام في بيئات الاختبار المصرح بها فقط**.  
> يُمنع تمامًا استخدامها ضد أنظمة دون إذن صريح.  
> المطور غير مسؤول عن أي سوء استخدام.

## 📦 المتطلبات

- Python 3.6 أو أحدث
- حزمة `requests`
- حزمة `colorama` (لدعم الألوان في Windows)

### التثبيت

```bash
pip install requests colorama
```

## ▶️ الاستخدام

### المسح الأساسي

```bash
python vuln_r2s_scanner.py http://target.com:3000
```

### المسح مع الوضع التفاعلي (يُفعّل تلقائيًا عند التأكيد)

```bash
python vuln_r2s_scanner.py http://target.com:3000 -i
```

### تحديد مجلد التقارير

```bash
python vuln_r2s_scanner.py http://target.com:3000 -o ./my_reports
```

## 💡 مثال على الإخراج

### عند الاكتشاف:
```
2025-12-12 22:30:05,752 [INFO] [HIGH] http://target:3000/ | Score: 90
```

### عند التأكيد:
```
2025-12-12 22:30:08,120 [INFO] [CRITICAL] http://target:3000/ | Score: 100 ✅ RCE CONFIRMED
```

### الوضع التفاعلي:
```
[*] Interactive Safe Command Shell
Available commands:
  id → id
  ls → ls -la /app 2>/dev/null || ls -la /
  env → printenv
  exit → Exit shell

vulnquest> id
uid=1001(node) gid=1001(node) groups=1001(node)
```

## 📁 هيكل التقرير

بعد الانتهاء من المسح، يتم إنشاء مجلد `output/reports` يحتوي على:

- `r2s_findings.json` — بيانات JSON خام للتكامل مع أدوات أخرى.
- `r2s_report.html` — تقرير مرئي يحتوي على نقاط الضعف المكتشفة.

## 🔬 آلية العمل

1. **الاكتشاف**:  
   المسح يبحث عن مؤشرات Next.js (مثل `x-nextjs` headers أو نص في HTML).

2. **التحقق**:  
   يُرسل طلبات POST مصممة خصيصًا لاستغلال:
   - `__proto__` pollution
   - `constructor` prototype manipulation
   - `Next-Action` header abuse

3. **الاستغلال الآمن**:  
   عند التأكيد، يُفعّل وضعًا تفاعليًا يسمح بتنفيذ أوامر **محدودة وغير ضارة** فقط.

## 📜 الترخيص

هذا المشروع مرخص بموجب [MIT License](LICENSE).

## 🙏 الاعتراف

- يعتمد على أبحاث أمنية حول ثغرة **Next.js RSC RCE** (CVE-2024-51378).
- مُلهم من أدوات مثل `nuclei` و`dalfox` في تصميم واجهة المستخدم.

## 📬 التواصل

للاستفسارات أو الإبلاغ عن مشكلات:  
- أنشئ **Issue** في هذا المستودع.
- لا ترسل طلبات استغلال حقيقية دون إذن!

> ✨ **VQ-R2S Scanner — دقة، أمان، واحترافية في كل مسح.**

